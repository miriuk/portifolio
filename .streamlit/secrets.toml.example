# Copy to .streamlit/secrets.toml locally (git-ignored), or paste into
# Streamlit Community Cloud -> app -> Settings -> Secrets.
# Only needed for the "Include Claude trader" option.
ANTHROPIC_API_KEY = "sk-ant-..."
